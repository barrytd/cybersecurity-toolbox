#!/usr/bin/env bash
# sysinfo.sh
# ----------
# Prints a quick snapshot of the host: OS, kernel, uptime, logged-in users,
# disk usage, and memory usage. Useful as a first-step recon script after
# landing on a new box.
#
# Planned commands: uname, hostnamectl, uptime, who, df -h, free -h
#
# Usage (planned):
#     ./sysinfo.sh
