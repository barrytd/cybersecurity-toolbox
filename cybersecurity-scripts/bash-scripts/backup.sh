#!/usr/bin/env bash
# backup.sh
# ---------
# Creates a timestamped tar.gz backup of a target directory. Designed to be
# safe to run from cron.
#
# Planned commands: tar, date, mkdir
#
# Usage (planned):
#     ./backup.sh <source-dir> <dest-dir>
