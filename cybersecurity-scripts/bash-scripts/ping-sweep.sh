#!/usr/bin/env bash
# ping-sweep.sh
# -------------
# Pings every host in a /24 subnet and prints the IPs that respond. A simple
# host-discovery script (the bash answer to nmap -sn).
#
# Planned commands: ping, for-loop, & (background jobs), wait
#
# Usage (planned):
#     ./ping-sweep.sh 192.168.1
#
# Educational use only. Only sweep networks you have permission to scan.
